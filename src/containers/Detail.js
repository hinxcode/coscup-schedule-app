import React from 'react';
import RX from 'reactxp';

export default class Detail extends RX.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <RX.ScrollView>
      </RX.ScrollView>
    );
  }
}
